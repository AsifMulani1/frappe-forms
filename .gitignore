# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.py~

# Distribution / packaging
.Python
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
tags
MANIFEST

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Dependency directories
node_modules/
jspm_packages/

# IDEs and editors
.vscode/
.vs/
.idea/
.kdev4/
*.kdev4
*.DS_Store
*.swp
*.comp.js
.wnf-lang-status
*debug.log

# Helix Editor
.helix/

# Aider AI Chat
.aider*

# Built SPA assets (forms/public/frontend/) are intentionally committed: `bench build` does NOT
# run the Vite build, so the production install relies on the checked-in bundle. Rebuild with
# `cd frontend && yarn build` after frontend changes (CI verifies the build stays green).

# React visual-reference prototype — not part of the shipped app.
design-reference/

# Local QA / dev artifacts (Playwright MCP output, ad-hoc screenshots).
.playwright-mcp/
qa-dashboard.png
